# Window 项目要求

## 产品定位

Window 是一个只负责 macOS 窗口管理的轻量后台工具。项目必须始终优先考虑低内存占用、低 CPU 占用、快速启动和行为可靠。

## 必须遵守的约束

- 使用系统原生框架，避免引入第三方依赖和不必要的运行时。
- 应用不需要主窗口、设置窗口、Dock 图标或菜单栏图标。
- 空闲状态不得轮询；功能应由全局快捷键事件驱动。
- 窗口操作必须针对用户当前聚焦的窗口，并适配多显示器、菜单栏和 Dock 占用的区域。
- 需要辅助功能权限时，只使用 macOS 标准授权流程。
- 新功能不得破坏既有快捷键，且应避免增加常驻对象和后台任务。

## 当前快捷键

- `Command + ↑`：最大化当前窗口至所在屏幕的可用区域。
- `Command + ←`：当前窗口置于左半屏。
- `Command + →`：当前窗口置于右半屏。
- `Command + ↓`：当前窗口调整为 998 × 836，并在屏幕可用区域内水平、垂直居中。
- `Command + Option + ↓`：保留当前聚焦窗口，隐藏其他应用并最小化当前应用的其他窗口。

## 验收要求

- Debug 与 Release 配置均可成功构建。
- 首次启动能触发辅助功能授权提示，授权后快捷键可用。
- 应用启动后不出现窗口、Dock 图标或菜单栏图标。
- 在不同尺寸和排列方式的显示器上验证窗口位置。
